import React from 'react';

const TodoForm = () => {
    return (
        <div>
            <input type='text' name='todo'/>
            <button>Add a todo</button>
        </div>
    )
}

export default TodoForm;